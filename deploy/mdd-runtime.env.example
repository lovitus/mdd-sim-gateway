# Copy outside the checkout, set the real host path, and keep this file private.
# The path must not be under the source checkout.
MDD_RUNTIME_ROOT=/var/lib/mdd-sim-gateway
MDD_BIND=0.0.0.0
MDD_PORT=8443
MDD_CONTROL_IMAGE=mdd-sim-gateway/control:latest
MDD_ENGINE_IMAGE=mdd-sim-gateway/engine:latest
